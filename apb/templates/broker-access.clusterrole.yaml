---

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: access-{{ broker_name }}-role
rules:
- nonResourceURLs:
    - "/ansible-service-broker"
    - "/ansible-service-broker/*"
  verbs:
    - "get"
    - "post"
    - "put"
    - "patch"
    - "delete"
