galaxy_info:
  author: David Zager
  description: Manage automation-broker in kubernetes/openshift cluster
  company: Red Hat, Inc

  # Some suggested licenses:
  license: Apache-2.0

  min_ansible_version: 2.5

  platforms:
    - name: Kubernetes
      versions:
        - 1.9
    - name: OpenShift
      versions:
        - 3.9

  galaxy_tags:
    - automation-broker
    - ansible-service-broker
    - apb

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
